#pragma once
#include <string>
#include <vector>

using Vbool = std::vector<bool>;
using VVbool = std::vector<Vbool>;
using VVVbool = std::vector<VVbool>;
using Vint = std::vector<int>;
using VVint = std::vector<Vint>;
using VVVint = std::vector<VVint>;
using Vfloat = std::vector<float>;
using VVfloat = std::vector<Vfloat>;
using VVVfloat = std::vector<VVfloat>;
using Vdouble = std::vector<double>;
using VVdouble = std::vector<Vdouble>;
using VVVdouble = std::vector<VVdouble>;
using string = std::string;
using Vstring = std::vector<string>;
using VVstring = std::vector<Vstring>;
using VVVstring = std::vector<VVstring>;